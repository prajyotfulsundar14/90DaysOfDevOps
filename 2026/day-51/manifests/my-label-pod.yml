apiVersion: v1
kind: Pod
metadata:
  name: mypod
  labels:
    app: myapp
    environment: dev
    team: frontend
spec:
  containers:
    - name: myapp-container
      image: nginx:latest
      ports:
        - containerPort: 80
